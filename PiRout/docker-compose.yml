version: "3"
services:
  api:
    build: ./api
    ports:
      - "5000:5000"
    volumes:
      - ./backend/python:/app/backend/python
  frontend:
    build: ./front-end
    ports:
      - "3000:80"
